employees = {}

for i in range(3):
    name = input("Enter employee's name: ")
    salary = input("Enter employee's salary: ")

    employees[name] = salary
print(employees)
unique={}
for name, salary in employees.items():
    if salary not in unique.values():
        unique[name] = salary


print(unique)
